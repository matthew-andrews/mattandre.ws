<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<%
	var title = page.title;

	if (is_archive()) {
		title = 'Archives';

		if (is_month()) {
			title += ': ' + page.year + '/' + page.month;
		} else if (is_year()) {
			title += ': ' + page.year;
		}
	} else if (is_category()) {
		title = 'Category: ' + page.category;
	} else if (is_tag()) {
		title = 'Tag: ' + page.tag;
	}
	%>
	<% if (title) { %>
	<title><%= title %> | <%= config.title %></title>
	<% } else { %>
	<title><%= config.title %> | Tokyo Product Manager, Executive MBA, recovering JavaScript, Golang developer</title>
	<% } %>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<%- open_graph({ twitter_id: config.twitter, updated: 'hacked' /* This switches off og:updated_time */ }) %>
	<% if (config.feed.path) { %>
		<link rel="alternate" href="/<%- config.feed.path %>" title="<%= config.title %>" type="application/atom+xml">
	<% } %>
	<%- css('main') %>
	<%- partial('google-analytics') %>
	<script data-ad-client="ca-pub-9198374266660020" async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
</head>
